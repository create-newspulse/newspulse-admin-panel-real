# 📁 .env.local.example

# =============================
# Admin API (local dev)
# =============================
# Preferred (proxy): leave VITE_ADMIN_API_ORIGIN empty and use the Vite proxy.
VITE_ADMIN_API_ORIGIN=
VITE_ADMIN_API_PROXY_BASE=/admin-api

# Vite dev proxy target (backend origin, NO /api suffix)
# Preferred (matches Vercel/prod data): use the real backend
VITE_ADMIN_API_TARGET=https://your-backend-host.tld

# Optional: local demo backend (only if you intentionally run it)
# VITE_ADMIN_API_TARGET=https://your-local-backend-host.tld

# Legacy / back-compat (optional)
# VITE_API_URL=https://your-backend-host.tld
# VITE_BACKEND_URL=https://your-backend-host.tld

# ✅ Founder Control Access (Frontend OK – not secret)
VITE_FOUNDER_PIN=NewsPulse#80121972

# 🚀 Founder Control mocks (enable during local dev unless backend is ready)
VITE_USE_MOCK=true

# 🔐 Backend-only (documented here; do not commit real values)
FOUNDER_ID=FOUND-001
FOUNDER_EMAIL=founder@example.com
FOUNDER_PIN_SALT=somesalt
EMERGENCY_CODE=NewsPulse#80121972
REACTIVATION_PHRASE=Kiran#80121972
BACKUP_BUCKET=newspulse-backups-dev
