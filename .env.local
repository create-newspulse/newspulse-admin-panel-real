VITE_USE_PROXY=true

# Local development backend target for the Vite proxy.
# No trailing "/api" and NEVER use placeholders like https://<render-backend>.onrender.com
VITE_ADMIN_API_TARGET=http://localhost:5000
