{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/admin-api/(.*)", "dest": "/api/admin-proxy/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
