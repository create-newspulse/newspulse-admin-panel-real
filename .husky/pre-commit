#!/usr/bin/env sh
# Minimal Husky pre-commit to avoid Windows PowerShell errors and not block commits
# This script intentionally does not source husky.sh to keep it portable.

echo "[husky] pre-commit: skipping heavy checks on Windows"
exit 0
